# Vault Configuration
VAULT_DATA_DIR=./data
VAULT_CONFIG_FILE=./data/vault_config.json

# Auth & Session Settings
SESSION_EXPIRE_MINUTES=30
MAX_FAILED_ATTEMPTS=5
LOCKOUT_DURATION_MINUTES=5

# Server Settings (if using FastAPI)
HOST=127.0.0.1
PORT=8000
